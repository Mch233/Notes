#if 

	if a:

	elif b:       #elif是else if的缩写

	else:

＃input
	
	s = input('birth: ')		＃input( )返回的数据类型是str
	birth = int(s)			＃int( )把str转换成整数

for

	for x in ...		#把每个元素代入变量x，然后执行缩进块的语句。

 list(range(5))		#range() 生成一个整数序列，再通过list()函数可以转换为list。

#while 

	while n > 0:
	    sum = sum + n
	    n = n - 2

#break

#continue


