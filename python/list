#llist　[  ]

	有序可变的集合　[  ]
	元素的数据类型可以不同，也可以是另一个list

	##访问
		用索引来访问list中每一个位置的元素
		索引是从0开始的，-1代表最后一个元素

	##方法

		长度
			
			len(name)

		尾插

			name.append('Adam')

		任意插入

			name.insert(1, 'Jack')

		删除元素    #pop(i),不加参数默认去尾

			name.pop(1)

		替换元素，直接赋值给对应的索引位置

			name[1] = 'Sarah'

＃tuple　(  )

	有序集合  (  )
	
	定义时必须初始化

	只有1个元素的tuple定义时必须加一个逗号

	元素
		元素的数据类型可以不同，也可以是另一个list
		指向不变; 　＃list元素　内容可变，指向不变
		只读不可写


